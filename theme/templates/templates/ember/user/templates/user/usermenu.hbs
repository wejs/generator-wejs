{{#if currentUser.id}}
<ul class="nav navbar-nav navbar-right">
  <li>
    <a href="javascript:;" class="dropdown-toggle user-menu-link" data-toggle="dropdown">
      <span class="user-name " >{{currentUser.displayName}}</span><b class="caret"></b>
    </a>
    <ul class="dropdown-menu">
      <li>
        <a href="#ChangeAvatarModal" {{action "showAvatarChangeModal"}} >
          {{t 'Change avatar'}}
        </a>
      </li>
      <li class="divider"></li>
      <li>
        {{#link-to 'user' currentUser.id}}
          {{t 'View / edit profile'}}
        {{/link-to}}
      </li>
      {{#if adminMenu}}
      <li>
        <a href="/admin">
          {{t 'menu.link.manage'}}
        </a>
      </li>      
      {{/if}}
      <li class="divider"></li>
      <li>
        <a href="/auth/logout" {{action 'logOut'}} class="">{{t 'Exit'}}</a>
      </li>
    </ul>
  </li>
</ul>
{{/if}}